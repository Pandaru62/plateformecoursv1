{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <h1>Editer une séquence</h1>

    {{ form_errors(form) }}

    {{ form_start(form) }}
        {{ form_row(form.numero) }}
        {{ form_row(form.titre) }}
        {{ form_row(form.description) }}
        {{ form_row(form.image) }}

        <button type="submit" class="btn btn-primary mt-3">Modifier</button>
    {{ form_end(form) }}
{% endblock %}
